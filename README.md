# TP3  building 
## steps : 
    -> Imporiting bootstrab inside the base twig template
    -> create a contoller using php bin/console make:controller ProductController
    -> create a form using the command  php bin/console make:form ProductOrderType
    -> implement the form class -> based on the html from...  using the type forms && helpers like buildForm...
        -> add  needed attrbs and DATA VALIDATION 
    -> implement the controller -> don't forget hanleRequest() method if u do , nothing will happned 
        -> validate the  submission and validation ...
